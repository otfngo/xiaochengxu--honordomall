<view>
  <swiper indicator-dots="{{true}}" autoplay="{{true}}">
    <block wx:for="{{slideList}}" wx:key="{{index}}">
      <swiper-item>
        <image src="{{item.src}}"></image>
      </swiper-item>
    </block>
  </swiper>
</view>